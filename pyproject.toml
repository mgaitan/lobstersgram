[build-system]
requires = ["uv_build>=0.7,<0.12"]
build-backend = "uv_build"

[project]
name = "lobstersgram"
version = "0.1.0"
description = "Telegram bot that posts Lobsters links to Telegraph"
readme = "README.md"
authors = [{ name = "Martín Gaitán", email = "gaitan@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "feedparser>=6.0.12",
    "md-to-telegraph",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "rich>=14.2.0",
    "markdown-this",
]

[project.urls]
Homepage = "https://github.com/mgaitan/lobstersgram/tree/master/src/lobstersgram"
Repository = "https://github.com/mgaitan/lobstersgram"
Issues = "https://github.com/mgaitan/lobstersgram/issues"

[project.scripts]
lobstersgram = "lobstersgram.main:main"

[tool.uv.workspace]
members = ["packages/md-to-telegraph", "packages/markdown-this", "packages/markdown-web"]

[tool.uv.sources]
md-to-telegraph = { workspace = true }
markdown-this = { workspace = true }
markdown-web = { workspace = true }

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "ipython>=9.8.0",
    "markdown-web",
    "pytest>=8.3.5",
    "pytest-cov>=6.0.0",
    "ruff>=0.14.10",
]


[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",
    "W",      # pycodestyle (E, W) https://docs.astral.sh/ruff/rules/#pycodestyle-e-w
    "F",      # https://docs.astral.sh/ruff/rules/#pyflakes-f
    "I",      # isort
    "C90",    # https://docs.astral.sh/ruff/rules/#mccabe-c90
    "A",      # https://docs.astral.sh/ruff/rules/#flake8-builtins-a
    "ANN",    # https://docs.astral.sh/ruff/rules/#flake8-annotations-ann
    "UP",     # https://docs.astral.sh/ruff/rules/#pyupgrade-up
    "RUF",    # https://docs.astral.sh/ruff/rules/#ruff-specific-rules-ruf
    "T10",    # remove pdbs
    "ISC",    # https://docs.astral.sh/ruff/rules/#flake8-implicit-str-concat-isc
    "SIM",    # https://docs.astral.sh/ruff/rules/#flake8-simplify-sim
    "ASYNC",  # https://docs.astral.sh/ruff/rules/#flake8-async-async
    "ERA",    # https://docs.astral.sh/ruff/rules/#eradicate-era
    "TRY",    # https://docs.astral.sh/ruff/rules/#tryceratops-try
    "BLE",    # https://docs.astral.sh/ruff/rules/#flake8-blind-except-ble
    "B",      # https://docs.astral.sh/ruff/rules/#flake8-bugbear-b
    "EXE",    # https://docs.astral.sh/ruff/rules/#flake8-executable-exe
    "FA",     # https://docs.astral.sh/ruff/rules/#flake8-future-annotations-fa
    "C4",     # https://docs.astral.sh/ruff/rules/#flake8-comprehensions-c4
    "N",      # https://docs.astral.sh/ruff/rules/#pep8-naming-n
    "PIE",    # https://docs.astral.sh/ruff/rules/#flake8-pie-pie
    "PERF",   # https://docs.astral.sh/ruff/rules/#perflint-perf
    "PL",     # https://docs.astral.sh/ruff/rules/#pylint-pl
    "FURB",   # https://docs.astral.sh/ruff/rules/#refurb-furb
    "RET",    # https://docs.astral.sh/ruff/rules/#flake8-return-ret
    "TID252", # https://docs.astral.sh/ruff/rules/relative-imports/
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib --cov=md_to_telegraph --cov=markdown_this --cov-report=term-missing --cov-fail-under=100"

[tool.coverage.run]
source = ["md_to_telegraph", "markdown_this"]

[tool.coverage.report]
fail_under = 100
