# Markdown Web

> Extract web pages to Markdown and publish Markdown, HTML, or URLs to Telegraph.

Base URL: {{ site_url }}

## Agent guidance

- Use `POST /md` when you need Markdown back.
- Use `POST /t` when you want a public Telegraph URL.
- Use `GET /md/<url>` or `GET /t/<url>` for a URL, with the complete source URL URL-encoded as one path segment.
- Prefer JSON for agents. The server accepts one of `url`, `html`, or `markdown` in the request body.
- `POST /t` returns JSON in the form `{"url":"https://telegra.ph/..."}`.
- `GET /t/<url>` returns a `303` redirect to the Telegraph page and caches that URL during the running process.
- A source that blocks server-side fetches should be sent as browser HTML to `POST /t/bookmarklet`; see `/bookmarklet/`.
- Do not send the service's Telegraph token. It uses its configured account unless an explicit bearer token is provided for `POST /t`.

## Markdown input

`markdown` may begin with YAML front matter. It must start with `---` on the first line and close with another `---` line. Accepted scalar keys are:

```yaml
---
title: Article title
author: Author name
url: https://example.com/article
date: 2026-08-10
image: https://example.com/cover.jpg
type: article
---

Article body in Markdown.
```

`title`, `author`, `url`, `date`, `image`, and `type` are the only recognized keys; nested values and unknown keys are ignored. A `title` or first Markdown heading is required to publish. Non-document types such as `website`, `home`, `collection`, `search`, and `landingpage` are rejected.

## Briefs with article cards

Use an exact lowercase `card` image marker wherever a curated article should appear:

```markdown
# Weekend brief

Editorial context.

![card](https://example.com/article)
```

When this Markdown is sent to `POST /t`, the service extracts and publishes each
marked source as its own Telegraph page. It replaces every marker with a linked
image, title, introduction, and Telegraph link, then adds previous, brief, and
next navigation to the published articles. Marker order determines navigation.
Repeated source URLs reuse one article page within that brief.

## Examples

```bash
curl -X POST {{ site_url }}/t \
  -H 'content-type: application/json' \
  -d '{"markdown":"---\ntitle: Hello\n---\n\n# Hello\n\nBody"}'

curl -X POST {{ site_url }}/t \
  -H 'content-type: application/json' \
  -d '{"markdown":"# Weekend brief\n\n![card](https://example.com/article)"}'

curl -X POST {{ site_url }}/md \
  -H 'content-type: application/json' \
  -d '{"url":"https://example.com/article"}'
```

## Machine-readable contract

The service publishes its OpenAPI 3.1 document at `{{ site_url }}/openapi.json` and interactive documentation at `{{ site_url }}/docs`. There is no separate `openschema.json`; use the OpenAPI document as the source of truth.
